# Cursor 总控 Prompt v2（徽派建筑可视化项目：按最新截图纠偏重构版）

> 这是一份给 Cursor 的**总控级主提示词**。  
> 目标不是让你一口气输出所有代码，而是让你先**重新理解项目真实意图**，承认旧文档与新截图之间存在冲突，然后以**最新截图作为布局真相、以文档作为数据语义与交互补充**，对当前 React + TypeScript 项目做一次**纠偏重构**。  
> 你必须在充分审计当前仓库后，先给出计划，再分阶段安全改造。  
> 不允许继续沿着已经偏掉的理解盲目推进。

---

## 0. 当前你必须接受的事实

这个项目此前已经有一部分骨架和 MVP 被实现出来，但当前实现和真实 UI 意图仍有偏差。  
请先接受以下前提：

### 0.1 已完成的阶段（你要基于这些成果继续，而不是推倒重来）
当前项目大致已经完成了以下内容：

- 阶段 1 / Prompt B：`frontend/` 下 React + TypeScript 工程骨架已落地
- 已有 `AppShell`、`TopNav`、React Router 路由
- 已有 `theme/tokens.ts`、公共 `ChartPanel`
- Prompt C：模块一 MVP 已实现一版
  - `ProvinceHeatMap`
  - 城市 hover / click
  - 重点建筑点位 hover / click
  - 点击城市联动 `DynastyLineChart`
  - 右侧 `BuildingInfoPanel`
  - mock 数据在 `frontend/src/modules/module1/data/*`
- Prompt D：模块二外壳 + 页面切换器已有一版，但当前实现方向不完全正确
- Prompt E：马头墙 Sankey 已有一版，组件路径大致如下：
  - `frontend/src/modules/module2/components/SankeyStory.tsx`
  - `frontend/src/modules/module2/data/horse-head-wall/sankey.json`
  - `frontend/src/modules/module2/subpages/HorseHeadWall/HorseHeadWallPage.tsx`

### 0.2 不能继续按照旧理解机械推进
当前项目的核心问题不是“代码没写完”，而是**设计真相发生了修正**：

- 早期文档里有些表述是对的，比如颜色、数据含义、图表种类、交互语义；
- 但在页面布局、模块二切换器位置和整体版式上，**最新截图比旧文字说明更接近真实 UI**；
- 所以不能继续把旧文字说明当唯一依据。

### 0.3 你现在的核心任务
你当前不是要“继续 Prompt F 往后堆功能”，而是要：

1. **先审计当前仓库**
2. **承认并吸收最新截图作为高优先级真相**
3. **在尽量复用已有代码的前提下重构模块布局**
4. **修正模块一、模块二、模块三的页面结构**
5. **然后再补齐各子图表和交互**

---

## 1. 最高优先级规则：信息冲突时的裁决顺序

这个项目当前存在“文档和截图不完全一致”的情况。  
你必须按照下面的优先级判断什么是最终真相：

### 1.1 真相优先级
当信息冲突时，请严格按这个顺序决策：

1. **用户最近一次提供的截图 / 页面草图**
2. **用户在对话中最新的口头更正**
3. **早期 docx / pdf 中的布局描述**
4. **早期 prompt 中的推断性说明**
5. **你自己为了实现方便做的假设**

### 1.2 具体应用到当前项目
这意味着：

- 模块二切换器：  
  旧文档写过“右下角四分之一圆”，但用户后续给了更明确的最新页面草图，显示其真实布局更接近**左下角 / 左下到中下的大扇形结构**。  
  所以请以**最新截图**为准，而不是死守旧文档的“右下角”。

- 模块一布局：  
  旧文档只描述了导航、地图、折线、右侧介绍。  
  但最新截图显示模块一页面实际上还包含：
  - 左上结构分类环图 / 甜甜圈图
  - 左下折线/面积图
  - 中间大地图 + 右上 inset 地图
  - 右侧介绍区
  - 下方大信息条带 / 详情板块  
  所以布局必须按最新截图重构，而数据含义仍可沿用文档。

- 模块三布局：  
  旧文档主要讲了“三雕介绍卡片”“营建先哲 / 珍籍撷英”“旭日图 / 玫瑰图”。  
  但最新截图给出了更具体的组合方式，所以布局上要以截图为准，文档用于补充卡片信息结构、配色和交互。

### 1.3 一个重要原则
**截图负责“长什么样、放在哪里、页面怎么组合”；  
文档负责“图表表达什么、字段是什么、颜色和交互如何定义”。**

你必须把两者合起来理解，而不是二选一。

---

## 2. 你的角色与工作方式

你现在是这个项目的：

- 前端主程
- 可视化工程师
- UI 纠偏实现者
- 架构与重构协作者

你必须遵守以下工作方式：

### 2.1 先审计，再写代码
每次开始执行前，先：

1. 扫描当前目录结构
2. 看现有 router / layout / components / modules
3. 查已有 `module1 / module2 / module3` 的实现程度
4. 判断哪些代码可以保留、哪些要重构、哪些要废弃
5. 给出风险说明

### 2.2 先出计划，再改动
每次不要直接埋头生成大堆代码。  
请先输出：

- 你对当前任务的理解
- 本轮要改的页面 / 组件 / 数据
- 改动原因
- 具体文件清单
- 风险点
- 验证方式

然后再动代码。

### 2.3 优先重构，不要盲目扩展
在当前阶段，优先级是：

1. 纠正模块一布局和地图表达
2. 纠正模块二扇形切换器与页面骨架
3. 搭好模块三真实版式骨架
4. 再补天井 / 粉墙 / 屋顶 / 水圳等详细图表
5. 最后整体打磨

### 2.4 组件化与数据解耦
请始终坚持：

- 页面负责布局
- 组件负责展示
- hooks / store 负责状态
- adapters / schema 负责数据转换
- 不要把所有 option 和布局塞进一个页面文件

### 2.5 不要乱猜真实数据
当前很多图表只是有结构、语义、示意图，没有完整真实数据。  
所以你可以先做 mock，但必须：

- mock 数据结构正确
- 文件独立
- 在代码中明确 `TODO: replace with real dataset`
- 在说明中指出哪些图是 mock

---

## 3. 当前仓库的目标技术栈与约束

### 3.1 默认栈
如果仓库已经是 React + TypeScript，就继续沿用。  
不要无意义切换到 Vue。  
默认沿用：

- React 18
- TypeScript
- Vite
- React Router
- Zustand 或当前已有轻量状态方案
- ECharts 作为主图表引擎
- 必要时少量自定义 SVG / CSS 结构
- 不要为一个效果引入过多依赖

### 3.2 图表选型建议
优先：
- ECharts：地图、折线、旭日图、Sankey、热力图、雷达图、玫瑰图、散点/气泡图
- 自定义 SVG / CSS：模块二左下扇形图片切换器、某些装饰统计图、时间条带
- Canvas 只在必须时使用

### 3.3 当前已有代码要尽量复用
能保留的尽量保留，例如：
- `AppShell`
- `TopNav`
- `ChartPanel`
- 已有 `SankeyStory`
- 已有 `module1` 数据结构
但保留不等于维持旧布局，必要时要改组件输入和布局方式。

---

## 4. 页面整体视觉风格

### 4.1 风格关键词
整个项目不是科技大屏，也不是后台系统。  
风格必须是：

- 徽派、古朴、温润
- 宣纸 / 黛瓦 / 青砖 / 木色 / 米白
- 现代网页秩序感
- 不脏、不土、不廉价
- 有留白，有书卷气

### 4.2 顶部导航共性
最新截图统一显示顶部深色导航条。  
请保持：

- 左侧 home 图标
- 中间三个一级模块名：
  - 徽韵千年
  - 构筑万象
  - 匠艺流芳
- 背景深灰：参考 `#4A4A48`
- 文字接近白色：`#FCFCFC`
- hover / active 有微弱高亮，但不要做成按钮 tab 风格过重

### 4.3 页面画布
整体背景偏浅灰白 / 米白，推荐：
- `#F2EFE9`
- `#F8F7F3`
- `#FCFCFC`
不同模块可稍调，但不要完全换风格。

---

## 5. 模块一：以最新截图为准重构，而不是沿用当前 MVP 版式

---

## 5.1 模块一真实目标（按最新截图）
模块一最终页面不应只是“左说明 + 中地图 + 右介绍 + 下折线”这种粗糙三段式。  
请按最新截图理解为：

### 顶部
- 全局导航条

### 主体上半区
分成三列，但左列内部再上下分块：

#### 左列上方
一个**建筑类型 / 用途分类甜甜圈图或环图**
从截图看，展示了民居、书院、牌坊、祠堂、亭台楼阁等类别。  
可理解为：
- 当前区域 / 当前筛选下的重要建筑或徽派建筑样本按建筑类型分类的占比图
- 环图需要中文标签和引线
- 视觉上是页面左侧上方的重要图表之一

#### 左列下方
一个**折线 / 面积图**
从截图看是随时间起伏的面积折线图。  
这里可以结合旧文档中的“历朝数量折线图”做语义落地：
- 横轴：时间 / 朝代 / 年份分段
- 纵轴：数量
- 颜色柔和偏蓝
- hover 有 tooltip

#### 中列
一个**主地图区域**
要求：
- 不是抽象拼块
- 必须体现真实地图空间边界
- 至少能清楚看出区域轮廓与城市层级
- 地图上有重要建筑 icon
- 右上有一个 inset 小地图 / 全域定位图
- 主地图是页面上半区的主视觉中心

#### 右列
一个**右侧介绍 / 图文说明区**
初始态可显示总体介绍；
点击地图建筑或城市后切换内容。

### 主体下半区
一个**宽幅的信息条带 / 详情板块**
从截图看包含：
- 筛选标签 / 题签
- 时间轴或条形滚动区域
- 左下文案区
- 底部长条图形 / 建筑立面式时间条带  
这一块旧文档没有写清，但**最新截图明确存在**，所以必须搭结构。
你可以先做成：
- 上部：标签筛选 + 标题信息
- 中部：横向滚动时间 / 建筑列表
- 下部：叙事性详情区域 / 示例图带
先用 mock 占位，但布局必须有。

---

## 5.2 模块一的数据语义（继续沿用旧文档）
虽然布局要按新图，但模块一的图表语义仍可沿用旧文档：

### 地图热力
- 展示浙江、安徽、江西三省范围
- 按城市着色或按城市点热力表达
- 指标：截止 1911 年各地徽派建筑总数量
- 颜色从深到浅：
  - `#4a848a`
  - `#67b8bf`
  - `#81e8ef`
  - `#e1f8fa`

### 重点建筑点位
- 设置建筑 icon，不要只是小圆点
- hover 显示建筑名称
- click 更新右侧内容
- icon 主色参考 `#4a4a48`

### 折线图
- 旧文档要求横轴朝代、纵轴数量，颜色 `#F7A072`
- 但最新截图左下图是蓝色面积折线  
=> 处理方式：  
  布局和视觉形式优先贴近截图；  
  数据语义可以延续“历朝数量变化”。  
  你可以把现有 `DynastyLineChart` 改造成更贴近截图的 area-line 视觉。

### 右侧说明区
- 初始态：徽派建筑总体介绍
- 点击建筑：建筑图片 + 介绍 + 标签

### 新增的左上环图
这个不是旧文档里明确写出的，但最新截图显示存在。  
请先将其定义为：
- 建筑类型占比 / 用途分类分布图
- 分类可先 mock：
  - 民居
  - 祠堂
  - 牌坊
  - 书院
  - 亭台楼阁
- 后续真实数据再替换

### 新增的下方宽幅详情板块
由于旧文档没完全说明，请按最新截图先做成：
- 结构存在
- 风格正确
- 可容纳后期真实内容
- 不要留白一大片

---

## 5.3 模块一工程要求
请重构模块一时满足：

1. 复用已有 `ProvinceHeatMap` 思路，但重做地图渲染与布局
2. 地图层必须支持：
   - 城市 hover
   - 城市 click
   - 建筑 marker hover
   - 建筑 marker click
3. 将左上环图、左下折线图、中间地图、右侧介绍、下方条带拆成独立组件
4. 不要把模块一页面写成 500 行大文件
5. 下方条带即使先 mock，也必须有布局骨架和假数据
6. 右上 inset 小地图必须有，不要忽略

---

## 6. 模块二：必须以最新截图作为新基准，彻底纠偏当前实现

---

## 6.1 模块二现在最大的问题
当前项目中模块二已经有：
- 外壳
- 切换器的一版
- 马头墙 Sankey 的一版

但当前问题是：
- 切换器形态不对
- 位置不对（按最新截图应在左下 / 左下到中下）
- 当前构件没有真正被“扇形主项化”
- 页面不是“一个构件一组图”，而是像“主图区只塞一张图”
- 版式没有贴近用户最新给出的真实目标图

### 结论
不要继续沿着当前错误版式做模块二。  
必须先把模块二页面骨架重构正确，再继续各子页内容。

---

## 6.2 模块二的真实版式（按最新截图）
模块二每个构件页都遵循同一大框架：

### 顶部
- 全局导航

### 主体右上大区
- 一张主图：**Sankey / 桑葚图**
- 几乎所有构件页右上都保留这张大图
- 即：模块二的共性主图就是“环境因子 → 功能需求 → 类型 → 结构 → 材料”的五层 Sankey

### 主体左下大区
- 一个**扇形图片切换器**
- 它不是普通 tab，不是按钮组，也不是小圆泡泡
- 它应该是一个**大面积扇形 / 四分之一圆式图片导航**
- 当前选中的构件图片最大
- 其余构件沿弧线排列
- hover 显示名称
- click 后交换位置
- 位置以**左下 / 左下至中下**为准，而不是右下角小泡泡

### 主体中下 / 右下
- 当前构件对应的另外 2~3 张图表
- 也就是说：
  - 每个构件页不是只显示一张图
  - 而是同时展示一组相关图表

---

## 6.3 模块二切换器的正确实现要求
这是非谈判项：

1. 左下放置
2. 大扇形结构
3. 当前 active 构件是最大扇区
4. 其余 4 个构件沿弧线排列
5. 点击某项后，该项与当前 active 项交换视觉主位
6. 不只是状态切换，而是**位置 + 大小 + 层级**都变化
7. 每个扇区内部使用缩略图 / 图片占位，不要纯色块
8. hover 显示：
   - 名称
   - 轻微描边 / 阴影
9. 最终组件建议命名为：
   - `StructureFanSwitcher`
   - 或 `QuarterFanImageSwitcher`
10. 当前已有 `QuarterImageSwitcher` 可以重构，但不要维持旧结构

---

## 6.4 模块二共用图：SankeyStory 继续复用，但版式要重新安放
所有构件的 Sankey 仍然是重要主图。  
继续保留通用组件思路：

```ts
type SankeyNode = {
  name: string
  category?: string
  description?: string
  itemStyle?: { color?: string }
}

type SankeyLink = {
  source: string
  target: string
  value: number
}
```

整体逻辑统一：
- 环境因子
- 功能需求
- 类型
- 结构
- 材料

统一色板参考旧文档：
- 环境因子：`#7a9e9f`
- 功能需求：`#5a7d63` 或各页变体
- 类型：`#8b5a2b` / 各页主题色
- 结构：`#b88662`
- 材料：`#9e7b4e`

但不同构件页允许根据文档做局部变色。

---

## 7. 模块二各子页：请按最新截图 + 文档语义来实现

---

## 7.1 马头墙页（最新截图 + 旧文档结合）
### 布局
- 右上：马头墙 Sankey（大）
- 左下：扇形切换器，当前为马头墙
- 中下：马头墙花纹样式旭日图
- 右下：马头墙词云

### 数据与图表
#### 1）Sankey
沿用《第二模块各个结构的桑葚图.docx》  
文档里已经给了比较完整的：
- 颜色
- 层级
- 节点
- links 示例
- hover / click 行为

#### 2）旭日图
沿用《第二模块马头墙花纹样式旭日图.docx》  
四层结构：
- 根：马头墙座头
- 一级：印斗式、坐吻式、鹊尾式、朝笏式
- 二级：具体纹样
- 三级：寓意
要按文档配色和层级半径做。

#### 3）词云
沿用《第二模块文化词云图.docx》中马头墙部分：
- 最大词：五岳朝天、马头墙
- 分类：意象核心 / 形态 / 功能 / 审美
- 字号公式：`min(72, 权重 * 13 + 16)`
- 布局：阶梯状、轻微 ±12° 旋转

### 当前阶段要求
即使真实数据不足，也要：
- 先做 mock 完整页面
- 版式先对
- 交互可分步加强

---

## 7.2 天井页
### 布局（按最新截图）
- 左上：关系矩阵热力图
- 右上：天井 Sankey
- 左下：扇形切换器，当前为天井
- 中下：天井尺度参数关联弦图
- 右下：天井词云
- 最右侧可有一张小雷达图，或做成热力图 / 雷达图切换模式

### 文档依据
来自《天井可视化设计图1，2 (1).docx》和词云文档。

#### 1）关系矩阵热力图
- 行：堂屋、卧室、厢房、走道、楼梯间
- 列：凹字形、H形、回字形、日字形、三角形
- 单元格值：0~1
- 要有 hover tooltip
- 有强度筛选滑块
- 有布局筛选器

#### 2）雷达图
- 与热力图共享状态
- 用于对比各布局平均连接强度
- 可以做成切换模式，也可以按最新截图做成右侧补充图

#### 3）弦图
- 18 个节点、6 类参数
- 支持筛选
- 真实数据不足先 mock，但数据转换函数必须独立

#### 4）词云
天井关键词：
- 四水归堂
- 肥水不流外人田
- 聚气藏风
- 纳气
- 采光
- 本源  
颜色按文档中的蓝绿灰系。

### 注意
天井页比马头墙更研究型。  
必须把：
- 热力图
- 雷达图
- 弦图
三者的数据层解耦，但状态关联。

---

## 7.3 粉墙页
### 布局（按最新截图）
- 左上：装饰元素统计图（圆形 / 跑道 + 外围波浪类视觉）
- 右上：粉墙 Sankey
- 左下：扇形切换器，当前为粉墙
- 中下：经典村落粉墙密度对比雷达图
- 右下：粉墙词云

### 文档依据
旧文档《文字介绍.docx》 + 词云文档。

#### 1）装饰元素统计图
要求表达：
- 人物
- 花草
- 鸟兽
- 几何纹样
及其数量和外围细分项  
如果完全复刻较难，可先用自定义 SVG / radial MVP 表达层级关系。

#### 2）雷达图
维度：
- 山墙
- 院墙
- 围墙
- 照壁
- 漏窗墙
村落：
- 宏村
- 西递
- 呈坎
- 南屏

#### 3）词云
关键词方向：
- 粉墙
- 黛瓦
- 留白
- 水墨晕染
- 清白
- 朴素
- 君子之风  
字体更文雅，旋转很轻。

---

## 7.4 屋顶页
### 布局（按最新截图）
- 左上：降水量 / 排水效率 / 屋顶坡度对比图（双柱 / 镜像条形）
- 右上：屋顶 Sankey
- 左下：扇形切换器，当前为屋顶
- 中下：装饰与符号半环图 / 半圆构件纹样图
- 右下：屋顶词云

### 文档依据
旧文档《文字介绍.docx》 + 词云文档。

#### 1）双向条形图
- 左侧：排水效率
- 中间：降水量
- 右侧：屋顶坡度
可按截图抽象做成左右镜像比较图。

#### 2）半环图
内圈：
- 脊饰
- 瓦当
- 滴水  
外圈：
- 人物
- 动物
- 植物
- 几何图样
- 吉祥寓意  
内圈点击外圈联动。

#### 3）词云
关键词：
- 如翚斯飞
- 飞檐
- 翘角
- 天际线
- 韵律
- 瓦当
- 小青瓦
- 滴水  
配色偏蓝绿 + 暖橙。

---

## 7.5 水圳系统页
### 布局（按最新截图）
- 左上：水圳示意图 / 引水结构说明图
- 右上：水圳 Sankey
- 左下：扇形切换器，当前为水圳系统
- 中下：雷达图或对比图
- 右下：说明性结构图 / 标签式信息图 / 水墨说明板

### 当前要求
即使文档最不完整，也不能留空。  
必须先有：
- 完整页面骨架
- mock 数据
- 占位图
- 后续接口约定

---

## 8. 模块三：不再只是占位，必须按最新截图真正落版

---

## 8.1 模块三真实布局（按最新截图）
模块三不是单纯一列内容，而是一个复合页面。

### 顶部
- 全局导航

### 上部区域：双卡片系统
从最新截图看，顶部实际上分成两块：

#### 左侧偏宽区域
- 一组“营建先哲”卡片
- 以横向排布方式出现
- 顶部有 tab / 切换按钮
- 卡片信息较长，支持滚动

#### 右侧偏窄区域
- 一组“珍籍撷英”卡片
- 同样有切换按钮
- 卡片布局更紧凑

> 如果截图的实际语义是“左大区域与右小区域同时展示两类卡片”，那么布局上请优先贴近截图。  
> 如果为了工程节奏先做成统一 tabs 切换的两个 panel，也可以，但最终视觉要尽量往截图靠。

### 中部主内容区域
最新截图中间是一块大的复合内容框，包含：

- 左侧：玫瑰图
- 中左：三雕技术旭日图
- 中间：当前三雕卡片（木雕/石雕/砖雕之一）
- 右侧：气泡 / 散点图

### 下部
- 另外两张三雕介绍卡片（非当前项）
- 当前项在中部主框中放大展示
- 其他两项在下方等待切换

这意味着模块三的三雕卡片系统不能再只是简单翻页组件，而要更接近一种：
- 主卡片 + 下方候选卡片 的结构

---

## 8.2 模块三图表与内容语义
### 1）营建先哲 / 珍籍撷英
依据《第三部分建筑科学家和著作卡片.docx》：

- 卡片大圆角
- 中式但现代
- 支持横向滚动
- 支持 hover 上移
- 支持内部纵向滚动
- 支持关键词交叉跳转
- 颜色：
  - 背景渐变：`#F2EFE9 / #E5E1D8`
  - 卡片背景：`#FCFCFC`
  - 青绿系按钮：`#B8D8D7 / #8EB8B7 / #77ACA9`
  - 强调橙：`#F7A072 / #F68E58`

### 2）三雕技术旭日图
依据《第三部分三雕技术旭日图玫瑰图.docx》：

四层：
- 一级：木雕 / 石雕 / 砖雕
- 二级：技艺 / 材料
- 三级：具体工艺 / 材料种类
- 四级：应用场景

配色：
- 木雕：暖木色系
- 石雕：青灰色系
- 砖雕：砖陶色系

### 3）玫瑰图
- 六县 × 三雕 = 18 扇形
- 半径表示数量
- 颜色对应三雕类型或县域分组
- 要放在页面左侧独立展示区

### 4）三雕介绍卡片
旧文档要求：
- 三张卡：木雕 / 石雕 / 砖雕
- 有简介
- 有工艺流程
- 切换时右侧散点图联动  
但最新截图进一步要求：
- 当前项在中部主框内作为主卡
- 其他两张卡在下方

### 5）气泡 / 散点图
题材维度：
- 人物
- 植物
- 动物
- 吉祥符号
- 民俗生活

应用色系：
- 牌坊
- 民居
- 祠堂
- 其他

颜色越深 / 点越大，频次越高。

---

## 8.3 模块三工程要求
1. 不再使用简单占位页
2. 先搭完整版式
3. 卡片系统、旭日图、玫瑰图、bubble chart 分开封装
4. 中部主框应可随 active craft 更新
5. 下方两张非 active craft 卡片点击后切换为主卡
6. “营建先哲 / 珍籍撷英”至少先做出真实卡片骨架与 mock 数据

---

## 9. 数据层与 schema 仍然必须先行

请继续保持数据分层，不要把 mock 写死在 JSX 中。

### 建议目录（可在现有基础上调整）
```txt
frontend/src/modules/module1/
  components/
  data/
  hooks/
  types.ts
  adapters.ts

frontend/src/modules/module2/
  components/
  data/
    horse-head-wall/
    patio/
    white-wall/
    roof/
    water-system/
  subpages/
  hooks/
  types.ts
  adapters.ts

frontend/src/modules/module3/
  components/
  data/
  hooks/
  types.ts
  adapters.ts
```

### 必要原则
- 原始数据和图表适配数据分层
- 可以有：
  - `buildModule1CategoryDonutData()`
  - `buildDynastyAreaSeries()`
  - `buildSankeyDataset()`
  - `buildPatioHeatmapMatrix()`
  - `buildPatioRadarSeries()`
  - `buildCarvingSunburstData()`
  - `buildRoseChartSeries()`

---

## 10. 你要立即执行的不是继续新功能，而是“纠偏重构计划”

---

## 10.1 当前最优先阶段
请立即执行以下阶段，而不是直接去做后续 Prompt F/J：

### 阶段 A：仓库审计 + 纠偏规划
你先输出：

1. 当前仓库目录结构
2. 已有模块实现情况
3. 哪些已有代码保留
4. 哪些需要重构
5. 哪些页面与最新截图偏差最大
6. 你建议的第一轮纠偏文件清单

### 阶段 B：模块一重构
目标：
- 版式贴近最新截图
- 左上环图、左下面积图、中间地图、右侧介绍、底部宽信息板全部到位
- 地图视觉不再是抽象拼块
- 右上 inset 小地图加入

### 阶段 C：模块二骨架重构
目标：
- 左下大扇形切换器
- 右上 Sankey 固定主位
- 下方副图区域到位
- 每个子页骨架统一
- 先把马头墙页改对，再复制模式到其它子页

### 阶段 D：模块三骨架重构
目标：
- 上部双卡片区
- 中部主框
- 左侧玫瑰图
- 中间旭日图
- 主 craft 卡片
- 右侧 bubble
- 下方两张 craft 卡片

### 阶段 E：再补子图细节和交互

---

## 11. 你必须给我的输出格式

每次回复都尽量按这个格式：

### 1）我对任务的理解
简要复述本轮目标

### 2）本轮纠偏点
列出本轮要修正的 UI 偏差

### 3）将修改 / 新增的文件
明确文件列表

### 4）实现说明
说明关键布局 / 数据 / 组件如何做

### 5）运行验证方式
告诉我怎么看效果

### 6）未解决问题
明确指出仍需用户补数据或确认的地方

---

## 12. 你不能做的事情

1. 不要继续沿用当前错误的模块二小泡泡切换器
2. 不要把模块一地图继续当抽象占位拼块
3. 不要忽略最新截图中的新增区域（模块一底部条带、模块三双卡片区等）
4. 不要为了省事只做一张图就说某个子页完成了
5. 不要将所有改动集中在一个页面大文件
6. 不要在没审计仓库前直接大规模覆盖已有代码
7. 不要假装文档无冲突，必须明确以截图优先
8. 不要给我空泛描述，必须落到具体文件与组件

---

## 13. 你应该优先复用 / 改造的组件建议

### 模块一
- `ProvinceHeatMap` → 改造成真实地图主图组件
- `DynastyLineChart` → 改造成更贴近截图的 area-line
- 新增：
  - `BuildingCategoryDonut`
  - `Module1BottomNarrativePanel`
  - `RegionInsetMap`

### 模块二
- `SankeyStory` → 继续保留并加强
- `QuarterImageSwitcher` → 重构为 `StructureFanSwitcher`
- 新增：
  - `StructureHeroFan`
  - `StructureWordCloudPanel`
  - `StructureSubChartFrame`
  - `HorseHeadWallSunburst`
  - `PatioHeatmap`
  - `PatioChordDiagram`
  - `WhiteWallDecorStats`
  - `RoofSlopeDrainageChart`
  - `RoofPatternSemiRing`
  - `WaterSystemOverviewCard`

### 模块三
- 新增或重构：
  - `ScholarCarousel`
  - `BookCarousel`
  - `CraftPrimaryCard`
  - `CraftSecondaryCard`
  - `BubbleMotifChart`
  - `CarvingSunburstPanel`
  - `CarvingRoseChart`

---

## 14. 当前你需要立即执行的第一条指令

现在请你**先不要继续原来的 Prompt F/J 流程**。  
请先严格按以下要求开始工作：

### 任务一：重新审计并提出纠偏重构方案
你需要：

1. 完整扫描当前 `frontend/` 目录
2. 列出已有布局、路由、模块组件、数据文件
3. 对照最新截图与旧文档，指出：
   - 模块一偏差
   - 模块二偏差
   - 模块三偏差
4. 给出第一轮重构计划，优先级为：
   - 模块一
   - 模块二
   - 模块三
5. 输出将要修改 / 新增的文件列表
6. 在我确认后再开始第一轮大改
7. 如果任务足够明确，也可以先做“最小安全改动”的第一轮重构

---

## 15. 一段更直接的执行摘要（你必须牢牢记住）

- 这不是从零开新项目，而是对现有 React 项目做**纠偏重构**
- 最新截图是布局真相，旧文档主要提供数据语义、颜色、交互细节
- 模块一要重做版式和地图表达
- 模块二要重做左下扇形切换器和整体骨架
- 模块三不能再占位，必须按新图搭真实结构
- 先纠偏，再扩展
- 先让页面结构对，再让图表细节对，再打磨动画和视觉

---

## 16. 你收到这份 Prompt 后的第一条回复模板（请按这个回）
请按下面格式回复我，不要直接贴大段代码：

### A. 当前审计摘要
- 技术栈
- 路由结构
- 已有模块情况

### B. 与最新截图相比的偏差列表
- 模块一偏差
- 模块二偏差
- 模块三偏差

### C. 第一轮重构计划
- 目标
- 保留哪些代码
- 重构哪些代码
- 新增哪些组件

### D. 文件变更清单
列出具体路径

### E. 风险与待确认项
如果有需要我补充的数据 / 图片 / 文案，请指出

在完成上面的回复之前，不要假装已经开始写完所有代码。

---

# 附：更细的子任务模板（后续你可以继续遵守）

## 子任务 1：模块一纠偏重构
请按最新截图重构模块一页面：
- 左上：建筑类型环图
- 左下：面积折线图
- 中间：主地图 + inset 小地图 + 建筑 icon
- 右侧：图文说明区
- 底部：宽幅叙事条带 / 信息板
保留旧文档的数据语义，但布局和视觉优先向截图靠齐。

## 子任务 2：模块二左下扇形切换器重构
请把当前模块二切换器从错误版本重构为左下大扇形图片切换器：
- 当前 active 最大
- 其他项沿弧线分布
- 点击交换位置
- 图片缩略图可先 mock
- 不再使用小泡泡按钮组

## 子任务 3：模块二统一子页骨架
请为马头墙 / 天井 / 粉墙 / 屋顶 / 水圳五个子页建立统一骨架：
- 右上 Sankey
- 左下扇形切换器
- 中下副图 1
- 右下副图 2
必要时加左上主图
每页结构相似，但图表内容不同。

## 子任务 4：模块三真实落版
请按最新截图搭模块三版式：
- 上部双卡片系统
- 中部复合图表区
- 左侧玫瑰图
- 中部旭日图
- 主 craft 卡片
- 右侧 bubble chart
- 下方两张 secondary craft 卡片

---

**现在开始：先审计当前仓库，并给出第一轮纠偏重构计划，不要直接铺天盖地生成全部代码。**


---

# 附录 A：更细的布局解释与组件拆分建议

这一节不是新的需求，而是为了避免你在实现时再次误解页面关系。  
请把它当成“布局拆解说明书”。

## A1. 模块一布局的真实阅读顺序

虽然模块一表面上是一个综合大屏，但用户阅读顺序应该是：

1. 先看顶部导航，明确当前在“徽韵千年”
2. 先扫到中间地图，理解地域分布
3. 再看左上环图，理解建筑类型构成
4. 再看左下面积折线，理解时间变化
5. 再看右侧说明区，理解当前建筑 / 当前城市的叙事内容
6. 最后看底部宽幅信息带，进入更细的案例与筛选

这意味着模块一不是单一“地图页面”，而是一个**地图驱动的综合叙事面板**。  
所以你不能把所有视觉力量都给地图，也不能把左上和左下做得过小。  
它们必须形成三角支撑关系：

- 中间地图：主视觉
- 左上环图：结构总览
- 左下折线：时间变化

## A2. 模块一推荐组件树
```txt
Module1Page
  ├─ TopNav
  ├─ Module1MainGrid
  │   ├─ LeftColumn
  │   │   ├─ BuildingCategoryDonutCard
  │   │   └─ DynastyAreaLineCard
  │   ├─ CenterMapColumn
  │   │   ├─ RegionInsetMap
  │   │   └─ ProvinceHeatMap
  │   └─ RightInfoColumn
  │       └─ BuildingInfoPanel
  └─ Module1BottomNarrativePanel
```

## A3. 模块一必须修正的视觉问题
当前旧版 MVP 最容易出现这些问题，你必须主动避免：

- 地图像抽象几何拼贴，不像真实地理空间
- 建筑点位太小，不像可交互对象
- 右侧介绍区只是大段文字，没有“卡片叙事感”
- 左上图和左下图像两个随手塞进去的小组件，没有版式主次
- 底部区域缺失，导致页面下半部空

你必须把模块一做成一个**有版心、有主次、有叙事节奏**的页面，而不是调试页。

---

## A4. 模块二布局的真实阅读顺序

模块二虽然有五个子页，但阅读逻辑几乎一致：

1. 先看到左下扇形切换器，知道当前浏览的是哪个构件
2. 视线移动到右上 Sankey，理解“环境 → 需求 → 类型 → 结构 → 材料”的因果链
3. 再看中下 / 右下的副图，理解该构件更细的文化 / 结构 / 类型特征
4. 通过 hover、click、切换器操作，实现构件页之间切换

所以模块二的切换器不是小附件，它是**子模块导航的主入口之一**。  
不要把它做成装饰品。  
它必须既承担导航，又承担“当前激活构件形象展示”的职责。

## A5. 模块二统一版心建议
所有模块二子页尽量遵守以下结构比例：

- 左下扇形切换器：占页面可用宽度约 26%~32%，高度约 38%~48%
- 右上 Sankey：占页面可用宽度约 52%~60%，高度约 42%~48%
- 中下副图：占页面可用宽度约 22%~28%
- 右下副图：占页面可用宽度约 28%~36%

不要每个子页都完全重新发明布局。  
要统一母版，再替换图表内容。

## A6. 模块二推荐骨架组件树
```txt
Module2Page
  ├─ TopNav
  ├─ Module2Canvas
  │   ├─ StructureFanSwitcher
  │   ├─ Module2TopPrimaryChartSlot
  │   ├─ Module2BottomSecondaryChartSlot
  │   ├─ Module2BottomTertiaryChartSlot
  │   └─ OptionalModule2LeftTopSlot
  └─ currentSubpageRenderer(activeStructure)
```

然后每个子页只负责为这些 slot 提供内容：

```txt
HorseHeadWallPage
  ├─ leftTop: optional overview image / trend chart
  ├─ topPrimary: SankeyStory
  ├─ bottomSecondary: HorseHeadWallSunburst
  └─ bottomTertiary: WordCloudPanel
```

```txt
PatioPage
  ├─ leftTop: PatioMatrixHeatmap
  ├─ topPrimary: SankeyStory
  ├─ bottomSecondary: ChordDiagram
  ├─ bottomTertiary: WordCloudPanel
  └─ extraRightMini: RadarCompareChart
```

## A7. 模块二中必须避免的错误
- 不要再把五个构件做成 tab 文本切换
- 不要把切换器缩成右下角小挂件
- 不要一个子页只放一张图就算完成
- 不要所有子页都完全不同排版，导致整体风格崩掉
- 不要把扇形切换器做成纯色扇区而没有图片感

---

## A8. 模块三布局的真实阅读顺序

模块三视觉层级最复杂，正确顺序是：

1. 顶部导航
2. 上部看“营建先哲 / 珍籍撷英”两类内容卡片
3. 中部看三雕相关复合主框：
   - 玫瑰图
   - 旭日图
   - 当前 craft 主卡
   - bubble chart
4. 下部再看到另外两张待切换 craft 卡片

这说明模块三不能只做“几块图并列”，而要做成**内容系统 + 图表系统的组合页面**。

## A9. 模块三推荐组件树
```txt
Module3Page
  ├─ TopNav
  ├─ Module3TopContentZone
  │   ├─ ScholarGalleryPanel
  │   └─ BookGalleryPanel
  ├─ Module3MainCompositeZone
  │   ├─ RoseChartPanel
  │   ├─ SunburstPanel
  │   ├─ ActiveCraftCard
  │   └─ BubbleMotifChart
  └─ Module3BottomCraftRail
      ├─ SecondaryCraftCard
      └─ SecondaryCraftCard
```

## A10. 模块三的重点不是“把所有内容都塞满”
你要克制：
- 给每块卡片足够留白
- 不要把上部卡片做得过高，压缩中部主图区域
- 中部复合主框是模块三最核心的展示区
- 下方两张 craft card 是“待切换项”，不能抢主位

---

# 附录 B：更细的数据类型与 mock 约定

当前很多图可以先用 mock，但 mock 必须规范。

## B1. 模块一数据类型建议
```ts
type BuildingCategoryDatum = {
  category: '民居' | '祠堂' | '牌坊' | '书院' | '亭台楼阁';
  value: number;
}

type CityHeatDatum = {
  cityId: string;
  cityName: string;
  province: '安徽' | '浙江' | '江西';
  totalBefore1911: number;
  center: [number, number];
}

type DynastyAreaPoint = {
  label: string; // 朝代或时间标签
  value: number;
}

type BuildingMarker = {
  id: string;
  name: string;
  cityId: string;
  coord: [number, number];
  image: string;
  summary: string;
  tags?: string[];
  dynasty?: string;
  type?: string;
}
```

## B2. 模块一底部宽幅叙事板 mock
如果真实需求还不清晰，请先做成下面结构：

```ts
type NarrativeFilterTag = {
  id: string;
  label: string;
}

type NarrativeCase = {
  id: string;
  title: string;
  subtitle?: string;
  type: string;
  era?: string;
  location?: string;
  description: string;
  score?: number;
}
```

底部区域可以先支持：
- 标签筛选
- 当前选中案例标题
- 横向滚动案例条带
- 下方长条形时间 / 建筑分布占位图

---

## B3. 模块二通用类型
```ts
type StructureTabKey =
  | 'horseHeadWall'
  | 'patio'
  | 'whiteWall'
  | 'roof'
  | 'waterSystem'

type StructureSwitcherItem = {
  key: StructureTabKey
  label: string
  image: string
  alt?: string
}
```

## B4. 模块二 Sankey 通用类型
```ts
type SankeyNode = {
  name: string
  category?: string
  description?: string
  itemStyle?: { color?: string }
}

type SankeyLink = {
  source: string
  target: string
  value: number
}
```

## B5. 词云通用类型
```ts
type WordCloudDatum = {
  text: string
  value: number
  category: string
  color?: string
  rotate?: number
}
```

## B6. 热力图与雷达图类型
```ts
type PatioLayoutType = '凹字形' | 'H形' | '回字形' | '日字形' | '三角形'
type PatioSpaceType = '堂屋' | '卧室' | '厢房' | '走道' | '楼梯间'

type HeatmapCell = {
  layout: PatioLayoutType
  space: PatioSpaceType
  value: number
  description?: string
}

type RadarSeriesDatum = {
  layout: PatioLayoutType
  values: Record<PatioSpaceType, number>
}
```

## B7. 弦图类型
```ts
type ChordNode = {
  id: string
  label: string
  group: string
  value?: number
}

type ChordEdge = {
  source: string
  target: string
  value: number
  cases?: string[]
}
```

## B8. 模块三类型
```ts
type CraftKey = 'wood' | 'stone' | 'brick'

type CraftCardData = {
  id: CraftKey
  title: string
  image: string
  summary: string
  process: string[]
}

type BubbleMotifDatum = {
  id: string
  craft: CraftKey
  theme: '人物' | '植物' | '动物' | '吉祥符号' | '民俗生活'
  application: '牌坊' | '民居' | '祠堂' | '其他'
  motif: string
  count: number
}
```

---

# 附录 C：逐模块验收清单（你每完成一轮都要自检）

## C1. 模块一验收
### 结构
- [ ] 顶部导航已存在
- [ ] 左上环图已存在
- [ ] 左下折线 / 面积图已存在
- [ ] 中间主地图已存在
- [ ] 右上 inset 小地图已存在
- [ ] 右侧图文介绍区已存在
- [ ] 底部宽幅叙事板已存在

### 交互
- [ ] hover 城市有反馈
- [ ] click 城市会更新图表或详情
- [ ] hover 建筑 icon 有名称
- [ ] click 建筑 icon 更新右侧信息
- [ ] 图表和详情不会白屏

### 视觉
- [ ] 地图不再像抽象拼块
- [ ] 主次关系清楚
- [ ] 版面不再有大面积无意义空白

## C2. 模块二验收
### 母版结构
- [ ] 左下扇形图片切换器存在
- [ ] 当前 active 项最大
- [ ] 右上 Sankey 主图区存在
- [ ] 中下副图区存在
- [ ] 右下副图区存在

### 交互
- [ ] hover 扇区显示名称
- [ ] click 扇区交换位置
- [ ] 切换子页内容同步变化
- [ ] Sankey hover / click 有反馈

### 子页覆盖
- [ ] 马头墙页三张图完成基本版
- [ ] 天井页三到四张图完成基本版
- [ ] 粉墙页三张图完成基本版
- [ ] 屋顶页三张图完成基本版
- [ ] 水圳页至少完成结构骨架和 mock

## C3. 模块三验收
### 结构
- [ ] 顶部双卡片区存在
- [ ] 中部复合主框存在
- [ ] 左侧玫瑰图存在
- [ ] 中部旭日图存在
- [ ] 主 craft 卡片存在
- [ ] 右侧 bubble chart 存在
- [ ] 下方 secondary craft cards 存在

### 交互
- [ ] 切换 craft 时主卡和图表联动
- [ ] 卡片 hover 有反馈
- [ ] 学者 / 著作卡片可滚动
- [ ] 关键词跳转机制有基础实现或接口预留

---

# 附录 D：你每轮改动后必须输出的“交付摘要模板”

请严格使用下面模板向我汇报：

## 本轮目标
一句话说明你这轮是修什么

## 已完成
- 修改了哪些页面
- 修改了哪些组件
- 修改了哪些数据文件

## 为什么这样改
- 对照最新截图的哪一点
- 对照旧文档的哪一点
- 哪些旧实现被保留，哪些被重做

## 我应该如何验证
- 打开哪个路由
- 看哪个区域
- 点击什么
- 预期效果是什么

## 目前仍缺什么
- 哪些是真实数据待补
- 哪些图片素材待补
- 哪些交互先用 mock

---

# 附录 E：一组更直接的短 prompt（你在总控 prompt 后可继续遵守）

## E1. 仓库审计 prompt
请先完整扫描当前 `frontend/` 仓库，不要直接开始大改。  
我要你告诉我：
1. 当前技术栈与依赖
2. 目录结构
3. 已有 `module1 / module2 / module3` 的真实实现情况
4. 哪些组件可复用
5. 哪些地方和最新截图冲突最大
6. 第一轮最安全的纠偏重构方案  
请输出结论摘要、详细分析、文件清单。

## E2. 模块一纠偏 prompt
请重构模块一页面，目标是贴近最新截图：
- 左上建筑类别环图
- 左下面积折线图
- 中间真实地图 + inset 小地图 + 建筑 icon
- 右侧详情卡
- 底部宽幅叙事区  
保留旧文档关于数据语义和颜色的要求，但布局与视觉以截图为准。  
请优先复用现有 `ProvinceHeatMap` 和 `DynastyLineChart`，必要时重写布局。

## E3. 模块二母版重构 prompt
请先不要继续做新的子图。  
请先把模块二母版重构正确：
- 左下大扇形图片切换器
- 右上 Sankey 主区
- 中下 / 右下副图区
- active 构件最大、点击交换位置  
然后再把马头墙页套进去做对。

## E4. 模块三版式落地 prompt
请把模块三从占位页重构为真实版式：
- 上部学者卡片区 + 著作卡片区
- 中部玫瑰图 + 旭日图 + 主 craft 卡片 + bubble chart
- 下部两张 secondary craft 卡片  
先用 mock 数据也可以，但布局必须贴近最新截图。

---

**再次强调：这不是让你继续往后堆功能，而是让你先纠偏重构，让整个项目重新回到正确轨道。**
